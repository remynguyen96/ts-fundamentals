{
  "name": "ts-fundamentals",
  "version": "1.0.0",
  "description": "Typescript Fundamentals",
  "main": "src/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "rm -rf dist && npx tsc --watch",
    "start": "npx tsc && node dist/$FILE",
    "dev": "rm -rf dist && tsc-watch --onSuccess \"node dist/src/GenericsInterface/index.js\""
  },
  "author": "Remy Nguyen",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.20"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.168",
    "tsc-watch": "^4.2.9",
    "typescript": "^4.1.3"
  }
}
